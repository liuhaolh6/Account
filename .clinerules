# 项目开发规则

## 1. 命名与规范
- 变量和函数命名语义化，使用驼峰命名法（camelCase），常量全大写。
- 保持代码简洁，避免过度设计，单文件代码尽量控制在 200 行以内。

## 2. 类型与安全
- 使用严格类型，绝对禁止使用 `any`，必要时使用 `unknown` 配合类型守卫。
- 禁止在代码中硬编码任何 API Key（如 DeepSeek Key），必须从环境变量或 `.env` 文件读取。

## 3. 错误处理
- 所有异步操作（尤其是调用 DeepSeek API 或网络请求）必须使用 `try-catch` 包裹。
- 错误信息必须用中文提示用户，禁止静默失败（即空的 catch 块）。

## 4. 注释与文档
- 关键逻辑必须添加简明扼要的中文注释，解释“为什么这么写”而不是“在做什么”。
- 每个对外暴露的函数，必须写清参数类型和返回值类型。
